export type Story = {
  storyID: number;
  opened: boolean;
  aspectRatio: number;
  backgroundColor: string;
  coverVideoPath?: string;
  coverImagePath?: string;
  title: string;
  titleColor: string;
};
